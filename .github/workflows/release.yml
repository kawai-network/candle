name: Release Libraries

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.1.0)'
        required: true
        default: 'v0.1.0'

permissions:
  contents: write

jobs:
  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: dtolnay/rust-toolchain@stable

      - name: Build library
        working-directory: candle_binding
        run: cargo build --release

      - name: Compress library
        run: |
          gzip -c candle_binding/target/release/libcandle_binding.so > libcandle_binding-linux-amd64.so.gz

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-libs
          path: libcandle_binding-linux-amd64.so.gz

  build-macos-arm:
    name: Build macOS ARM64
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: dtolnay/rust-toolchain@stable

      - name: Build library
        working-directory: candle_binding
        run: cargo build --release

      - name: Compress library
        run: |
          gzip -c candle_binding/target/release/libcandle_binding.dylib > libcandle_binding-darwin-arm64.dylib.gz

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm-libs
          path: libcandle_binding-darwin-arm64.dylib.gz

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build library
        working-directory: candle_binding
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Compress library
        shell: bash
        run: |
          gzip -c candle_binding/target/x86_64-pc-windows-msvc/release/candle_binding.dll > libcandle_binding-windows-amd64.dll.gz

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-libs
          path: libcandle_binding-windows-amd64.dll.gz

  release:
    name: Create Release
    needs: [build-linux, build-macos-arm, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'release' || github.event.inputs.version != ''
    steps:
      - uses: actions/checkout@v4

      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: linux-libs
          path: ./libs/

      - name: Download macOS ARM artifacts
        uses: actions/download-artifact@v4
        with:
          name: macos-arm-libs
          path: ./libs/

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-libs
          path: ./libs/

      - name: List files
        run: ls -la ./libs/

      - name: Upload to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: ./libs/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create release (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: Library Release ${{ github.event.inputs.version }}
          files: ./libs/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
